import HotelFilterService from './hotel-filter.service';
import MockData from '../hotel-mock-api-data';

describe('HotelFilterService', () => {
    
    it('exists', () => {
        expect(HotelFilterService).toEqual(3);
    });
});